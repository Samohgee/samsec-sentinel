version: '3.9'
services:
  api:
    build: .
    ports:
      - '8000:8000'
    env_file:
      - .env
    depends_on:
      - db
    command: uvicorn backend.app.main:app --host 0.0.0.0 --port 8000

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: samsec
      POSTGRES_USER: samsec
      POSTGRES_PASSWORD: samsec
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
