{
  "name": "samsec-labs",
  "version": "1.0.0",
  "private": true,
  "description": "SAMSEC LABS — cybersecurity and development services platform (FastAPI + Supabase).",
  "type": "module",
  "scripts": {
    "dev": "uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "uvicorn backend.app.main:app --host 0.0.0.0 --port 8000",
    "build": "node build.mjs"
  },
  "keywords": ["cybersecurity", "fastapi", "supabase"],
  "license": "MIT"
}
